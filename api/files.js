import {json,requireAuth} from './_security.js';
export default async function handler(req,res){if(!requireAuth(req,res))return;const base='https://api.telegram.org/bot'+process.env.TELEGRAM_BOT_TOKEN;if(req.method==='GET'){const r=await fetch(base+'/getChat',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({chat_id:process.env.TELEGRAM_CHAT_ID})});if(!r.ok)return json(res,502,{error:'Telegram storage unavailable'});return json(res,200,{storage:'telegram',chatId:process.env.TELEGRAM_CHAT_ID,ready:true});}if(req.method==='POST')return json(res,501,{error:'Upload route reserved for multipart handler',next:'Add Telegram sendDocument flow in api/upload.js'});return json(res,405,{error:'Method not allowed'});}
