import Busboy from 'busboy';import {sql} from '@vercel/postgres';import {json,auth,tg} from './_security.js';import {ensureDb} from './_db.js';
export const config={api:{bodyParser:false}};const MAX=Number(process.env.MAX_UPLOAD_MB||20)*1024*1024;
export default async function(req,res){if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});if(!auth(req,res))return;if(!process.env.TELEGRAM_BOT_TOKEN||!process.env.TELEGRAM_CHAT_ID)return json(res,503,{error:'Telegram storage is not configured'});let file=null,tooBig=false,parentId=null;const bb=Busboy({headers:req.headers,limits:{files:1,fileSize:MAX,fields:10}});bb.on('field',(n,v)=>{if(n==='parent_id')parentId=v||null});bb.on('file',(field,stream,info)=>{const chunks=[];let size=0;file={name:info.filename,type:info.mimeType};stream.on('data',c=>{size+=c.length;if(size<=MAX)chunks.push(c)});stream.on('limit',()=>tooBig=true);stream.on('end',()=>{file.buffer=Buffer.concat(chunks);file.size=size})});bb.on('finish',async()=>{try{if(tooBig)return json(res,413,{error:`File exceeds ${MAX/1024/1024} MB`});if(!file?.buffer?.length)return json(res,400,{error:'No file supplied'});const form=new FormData();form.append('chat_id',process.env.TELEGRAM_CHAT_ID);form.append('caption',`Saad Drive | ${file.name}`);form.append('document',new Blob([file.buffer],{type:file.type||'application/octet-stream'}),file.name);const r=await fetch(tg('sendDocument'),{method:'POST',body:form});const d=await r.json();if(!r.ok||!d.ok)return json(res,502,{error:'Telegram upload failed',detail:d.description});await ensureDb();const doc=d.result.document;const q=await sql`INSERT INTO drive_items(name,kind,parent_id,telegram_file_id,mime_type,size_bytes) VALUES(${file.name},'file',${parentId},${doc.file_id},${file.type||null},${doc.file_size||file.size}) RETURNING *`;return json(res,201,{item:q.rows[0]})}catch(e){return json(res,500,{error:'Upload failed',detail:e.message})}});req.pipe(bb)}
