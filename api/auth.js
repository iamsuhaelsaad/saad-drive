import {json,hashPin,sign} from './_security.js';
export default function(req,res){if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});let b='';req.on('data',x=>b+=x);req.on('end',()=>{try{const {pin}=JSON.parse(b||'{}');if(!process.env.APP_PIN_HASH||hashPin(pin)!==process.env.APP_PIN_HASH)return json(res,401,{error:'Invalid PIN'});json(res,200,{token:sign()})}catch{json(res,400,{error:'Invalid request'})}})}
