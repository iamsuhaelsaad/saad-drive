import {json,hashPin,tokenFor} from './_security.js';
export default function handler(req,res){if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});let body='';req.on('data',chunk=>body+=chunk);req.on('end',()=>{try{const {pin}=JSON.parse(body||'{}');if(!pin||!process.env.APP_PIN_HASH)return json(res,400,{error:'PIN service is not configured'});if(hashPin(pin)!==process.env.APP_PIN_HASH)return json(res,401,{error:'Invalid PIN'});return json(res,200,{token:tokenFor(),expiresIn:7200})}catch{return json(res,400,{error:'Invalid request'})}})}
